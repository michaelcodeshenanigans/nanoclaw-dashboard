---
phase: 03-group-detail-sessions
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/routes/groups/[id]/+page.svelte
autonomous: true
requirements:
  - GRP-02
  - GRP-03
  - GRP-06
  - GRP-08
must_haves:
  truths:
    - "Svelte 5 runes ($state, $effect, $derived) REQUIRED — NO writable, NO onDestroy"
    - "createPoller from $lib/poll is used for ALL polling"
    - "Poll interval is 5000ms for group + sessions, 10000ms for members + destinations"
    - "params.id is read from SvelteKit's $app/stores page store as Number($page.params.id)"
    - "Each card handles its own loading, error, and empty states"
    - "Dark card style: rounded-lg border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-6"
    - "Container status badge: running=bg-green-500, error=bg-red-500, stopped/null=bg-gray-500"
    - "Back link '← Groups' navigates to /groups using SvelteKit <a href> (no client-side router import needed)"
    - "Session row link goes to /sessions/${session.id}"
    - "formatDistanceToNow from date-fns is used for relative timestamps"
    - "Types Group, GroupDetail, Member, Destination, SessionSummary come from $lib/types (created in Wave 1)"
  artifacts:
    - path: "src/routes/groups/[id]/+page.svelte"
      provides: "GRP-02, GRP-03, GRP-06, GRP-08"
      contains: "Group detail page with header, config card, members card, destinations card, and sessions card. Uses four createPoller instances reading shared params.id; renders cards in vertical stack with dark theme."
---

<objective>
Build the group detail page at `/groups/[id]` that shows a single agent group's configuration, members, destinations, and recent sessions. Each section polls its own API endpoint independently so a slow members fetch doesn't block the sessions list from updating. The page is the primary deep-dive view for an operator looking at one agent group.
</objective>

<tasks>

<task type="auto">
<name>Create group detail page</name>
<files>
- src/routes/groups/[id]/+page.svelte
</files>

<action>
Create the file `src/routes/groups/[id]/+page.svelte` with EXACTLY the following content:

```svelte
<script lang="ts">
  import { page } from '$app/stores';
  import { createPoller } from '$lib/poll';
  import { formatDistanceToNow, format } from 'date-fns';
  import type {
    GroupDetail,
    Member,
    Destination,
    SessionSummary
  } from '$lib/types';

  const id = Number($page.params.id);

  const detail = createPoller<GroupDetail | null>(
    () => fetch(`/api/groups/${id}`).then((r) => (r.ok ? r.json() : null)),
    5000
  );
  const members = createPoller<Member[]>(
    () => fetch(`/api/groups/${id}/members`).then((r) => r.json()),
    10000
  );
  const destinations = createPoller<Destination[]>(
    () => fetch(`/api/groups/${id}/destinations`).then((r) => r.json()),
    10000
  );
  const sessions = createPoller<SessionSummary[]>(
    () => fetch(`/api/groups/${id}/sessions`).then((r) => r.json()),
    5000
  );

  function statusClass(status: string | null | undefined): string {
    if (status === 'running') return 'bg-green-500';
    if (status === 'error') return 'bg-red-500';
    return 'bg-gray-500';
  }

  function statusLabel(status: string | null | undefined): string {
    if (!status) return 'stopped';
    return status;
  }

  function formatTimestamp(ts: string | null | undefined): string {
    if (!ts) return '—';
    try {
      return format(new Date(ts), 'yyyy-MM-dd HH:mm');
    } catch {
      return ts;
    }
  }

  function relativeTime(ts: string | null | undefined): string {
    if (!ts) return '—';
    try {
      return formatDistanceToNow(new Date(ts), { addSuffix: true });
    } catch {
      return ts;
    }
  }
</script>

<svelte:head>
  <title>Group {id} · NanoClaw Dashboard</title>
</svelte:head>

<div class="mx-auto flex max-w-5xl flex-col gap-6 p-6">
  <!-- Header -->
  <div class="flex items-center justify-between">
    <div class="flex items-center gap-4">
      <a
        href="/groups"
        class="text-sm text-[hsl(var(--muted-foreground))] hover:text-[hsl(var(--foreground))]"
      >
        ← Groups
      </a>
      {#if detail.data}
        <h1 class="text-2xl font-semibold text-[hsl(var(--foreground))]">
          {detail.data.name}
        </h1>
        <span
          class="inline-flex items-center gap-2 rounded-full px-3 py-1 text-xs font-medium text-white {statusClass(
            detail.data.container_status
          )}"
        >
          {statusLabel(detail.data.container_status)}
        </span>
      {:else if detail.loading}
        <h1 class="text-2xl font-semibold text-[hsl(var(--muted-foreground))]">Loading…</h1>
      {:else if detail.error}
        <h1 class="text-2xl font-semibold text-red-500">Error</h1>
      {:else}
        <h1 class="text-2xl font-semibold text-[hsl(var(--muted-foreground))]">Not found</h1>
      {/if}
    </div>
  </div>

  <!-- Config card -->
  <section
    class="rounded-lg border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-6 text-[hsl(var(--card-foreground))]"
  >
    <h2 class="mb-4 text-lg font-semibold">Configuration</h2>
    {#if detail.loading && !detail.data}
      <p class="text-sm text-[hsl(var(--muted-foreground))]">Loading…</p>
    {:else if detail.error}
      <p class="text-sm text-red-500">Failed to load group: {detail.error.message}</p>
    {:else if !detail.data}
      <p class="text-sm text-[hsl(var(--muted-foreground))]">Group not found.</p>
    {:else}
      <dl class="grid grid-cols-1 gap-x-8 gap-y-3 sm:grid-cols-2">
        <div>
          <dt class="text-xs uppercase tracking-wide text-[hsl(var(--muted-foreground))]">
            Folder
          </dt>
          <dd class="mt-1 font-mono text-sm">{detail.data.folder ?? '—'}</dd>
        </div>
        <div>
          <dt class="text-xs uppercase tracking-wide text-[hsl(var(--muted-foreground))]">
            Agent provider
          </dt>
          <dd class="mt-1 font-mono text-sm">{detail.data.agent_provider ?? '—'}</dd>
        </div>
        <div>
          <dt class="text-xs uppercase tracking-wide text-[hsl(var(--muted-foreground))]">
            Created at
          </dt>
          <dd class="mt-1 font-mono text-sm">{formatTimestamp(detail.data.created_at)}</dd>
        </div>
        <div>
          <dt class="text-xs uppercase tracking-wide text-[hsl(var(--muted-foreground))]">
            Group ID
          </dt>
          <dd class="mt-1 font-mono text-sm">{detail.data.id}</dd>
        </div>
      </dl>
    {/if}
  </section>

  <!-- Members card -->
  <section
    class="rounded-lg border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-6 text-[hsl(var(--card-foreground))]"
  >
    <h2 class="mb-4 text-lg font-semibold">Members</h2>
    {#if members.loading && !members.data}
      <p class="text-sm text-[hsl(var(--muted-foreground))]">Loading…</p>
    {:else if members.error}
      <p class="text-sm text-red-500">Failed to load members: {members.error.message}</p>
    {:else if !members.data || members.data.length === 0}
      <p class="text-sm text-[hsl(var(--muted-foreground))]">No members</p>
    {:else}
      <table class="w-full text-sm">
        <thead>
          <tr class="border-b border-[hsl(var(--border))] text-left text-[hsl(var(--muted-foreground))]">
            <th class="py-2 font-medium">Name</th>
            <th class="py-2 font-medium">Platform</th>
            <th class="py-2 font-medium">Role</th>
          </tr>
        </thead>
        <tbody>
          {#each members.data as m (m.id ?? `${m.platform}:${m.name}`)}
            <tr class="border-b border-[hsl(var(--border))]/50">
              <td class="py-2">{m.name}</td>
              <td class="py-2 font-mono text-xs text-[hsl(var(--muted-foreground))]">
                {m.platform}
              </td>
              <td class="py-2">{m.role ?? '—'}</td>
            </tr>
          {/each}
        </tbody>
      </table>
    {/if}
  </section>

  <!-- Destinations card -->
  <section
    class="rounded-lg border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-6 text-[hsl(var(--card-foreground))]"
  >
    <h2 class="mb-4 text-lg font-semibold">Destinations</h2>
    {#if destinations.loading && !destinations.data}
      <p class="text-sm text-[hsl(var(--muted-foreground))]">Loading…</p>
    {:else if destinations.error}
      <p class="text-sm text-red-500">
        Failed to load destinations: {destinations.error.message}
      </p>
    {:else if !destinations.data || destinations.data.length === 0}
      <p class="text-sm text-[hsl(var(--muted-foreground))]">No destinations</p>
    {:else}
      <table class="w-full text-sm">
        <thead>
          <tr class="border-b border-[hsl(var(--border))] text-left text-[hsl(var(--muted-foreground))]">
            <th class="py-2 font-medium">Name</th>
            <th class="py-2 font-medium">Platform</th>
          </tr>
        </thead>
        <tbody>
          {#each destinations.data as d (d.id ?? `${d.platform}:${d.name}`)}
            <tr class="border-b border-[hsl(var(--border))]/50">
              <td class="py-2">{d.name}</td>
              <td class="py-2 font-mono text-xs text-[hsl(var(--muted-foreground))]">
                {d.platform}
              </td>
            </tr>
          {/each}
        </tbody>
      </table>
    {/if}
  </section>

  <!-- Sessions card -->
  <section
    class="rounded-lg border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-6 text-[hsl(var(--card-foreground))]"
  >
    <h2 class="mb-4 text-lg font-semibold">Recent sessions</h2>
    {#if sessions.loading && !sessions.data}
      <p class="text-sm text-[hsl(var(--muted-foreground))]">Loading…</p>
    {:else if sessions.error}
      <p class="text-sm text-red-500">Failed to load sessions: {sessions.error.message}</p>
    {:else if !sessions.data || sessions.data.length === 0}
      <p class="text-sm text-[hsl(var(--muted-foreground))]">No sessions</p>
    {:else}
      <table class="w-full text-sm">
        <thead>
          <tr class="border-b border-[hsl(var(--border))] text-left text-[hsl(var(--muted-foreground))]">
            <th class="py-2 font-medium">Thread ID</th>
            <th class="py-2 font-medium">Status</th>
            <th class="py-2 font-medium">Last active</th>
            <th class="py-2"></th>
          </tr>
        </thead>
        <tbody>
          {#each sessions.data as s (s.id)}
            <tr class="border-b border-[hsl(var(--border))]/50">
              <td class="py-2 font-mono text-xs">{s.thread_id ?? '—'}</td>
              <td class="py-2">
                <span
                  class="inline-flex items-center rounded-full px-2 py-0.5 text-xs font-medium text-white {statusClass(
                    s.container_status
                  )}"
                >
                  {statusLabel(s.container_status)}
                </span>
              </td>
              <td class="py-2 text-[hsl(var(--muted-foreground))]">
                {relativeTime(s.last_active)}
              </td>
              <td class="py-2 text-right">
                <a
                  href={`/sessions/${s.id}`}
                  class="text-sm text-[hsl(var(--accent-foreground))] hover:underline"
                >
                  View →
                </a>
              </td>
            </tr>
          {/each}
        </tbody>
      </table>
    {/if}
  </section>
</div>
```
</action>

<verify>
- File exists at `src/routes/groups/[id]/+page.svelte`
- File imports `page` from `$app/stores`, `createPoller` from `$lib/poll`, `formatDistanceToNow` + `format` from `date-fns`, and types from `$lib/types`
- File contains exactly four `createPoller(...)` calls — one each for `/api/groups/${id}`, `/api/groups/${id}/members`, `/api/groups/${id}/destinations`, `/api/groups/${id}/sessions`
- File contains NO `writable`, NO `onDestroy`, NO `onMount` imports
- File contains a back link `<a href="/groups">← Groups</a>`
- File contains four `<section>` elements (config, members, destinations, sessions), each using `rounded-lg border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-6`
- The status badge function returns `bg-green-500` for running, `bg-red-500` for error, `bg-gray-500` otherwise
- The sessions table rows link to `/sessions/${s.id}`
- `pnpm run check` (svelte-check) passes with no errors related to this file
</verify>

<acceptance_criteria>
- Visiting `/groups/1` (or any valid group id) shows the group name and container status badge in the header
- Config card shows folder, agent_provider, created_at
- Members card shows a table when there are members, "No members" when empty
- Destinations card shows a table when there are destinations, "No destinations" when empty
- Sessions card shows thread_id, status badge, relative last_active, and a View link to `/sessions/[id]` for each session
- Back link `← Groups` navigates to `/groups`
- All four cards independently render loading, error, and empty states without blocking each other
</acceptance_criteria>

<done>
- File `src/routes/groups/[id]/+page.svelte` written with exact content above
- All `<verify>` checks pass
</done>

</task>

</tasks>

<verification>
- [ ] `src/routes/groups/[id]/+page.svelte` exists
- [ ] File uses Svelte 5 runes only — no `writable`, no `onDestroy`, no `onMount`
- [ ] Four `createPoller` instances with correct URLs and intervals (5s detail, 10s members, 10s destinations, 5s sessions)
- [ ] `params.id` read via `$page.params.id` and coerced with `Number(...)`
- [ ] Header, Config, Members, Destinations, Sessions cards present with dark theme classes
- [ ] Status badge uses bg-green-500 / bg-red-500 / bg-gray-500 based on `container_status`
- [ ] Session rows link to `/sessions/${s.id}`
- [ ] Back link `← Groups` to `/groups`
- [ ] `pnpm run check` reports no errors for this file
</verification>

<success_criteria>
- GRP-02 satisfied: group config (folder, agent_provider, created_at, id) is rendered
- GRP-03 satisfied: members table renders name, platform, role
- GRP-06 satisfied: destinations table renders name, platform
- GRP-08 satisfied: active/recent sessions render with status, last_active, and link to session detail
- Page handles loading, error, empty, and populated states gracefully per card
</success_criteria>

<output>
After finishing the task, create `/workspace/agent/nanoclaw-dashboard/.planning/phases/03-group-detail-sessions/03-03-SUMMARY.md` containing:
- One-paragraph summary of what was built
- File created (path)
- Requirements satisfied (GRP-02, GRP-03, GRP-06, GRP-08)
- Verification results (pass/fail per `<verify>` bullet)
- Any deviations from the plan with justification
</output>
