---
phase: 01-foundation-deploy
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - svelte.config.js
  - vite.config.ts
  - tailwind.config.js
  - tsconfig.json
  - src/app.css
  - src/routes/+layout.svelte
  - src/routes/+page.svelte
  - static/.gitkeep
autonomous: true
requirements:
  - INFRA-03
must_haves:
  truths:
    - "SvelteKit 2.x with @sveltejs/adapter-node is the ONLY allowed full-stack framework — no Hono, no separate API server"
    - "Tailwind 4.x uses CSS-native variables, NOT a tailwind.config.js theme object — do NOT mix Tailwind 3 patterns"
    - "shadcn-svelte components land in src/lib/components/ui/ after init"
    - "Svelte 5 runes ($state, $effect, $derived) are required — do NOT use legacy Svelte 4 stores or onDestroy"
    - "The dark theme base is applied globally via CSS variables in src/app.css"
    - "Container port is 3000 (adapter-node default via PORT env) — do NOT hardcode 3001"
    - "pnpm is the ONLY package manager — do NOT use npm or yarn"
    - "The layout sidebar must include nav links for: Overview (/), Groups (/groups), Sessions (/sessions), Approvals (/approvals)"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "@sveltejs/adapter-node"
    - path: "svelte.config.js"
      provides: "SvelteKit configuration with adapter-node"
      contains: "adapter-node"
    - path: "vite.config.ts"
      provides: "Vite configuration with Tailwind 4 plugin"
      contains: "@tailwindcss/vite"
    - path: "src/app.css"
      provides: "Global dark theme CSS variables"
      contains: "@import \"tailwindcss\""
    - path: "src/routes/+layout.svelte"
      provides: "Dark sidebar nav shell"
      contains: "/groups"
    - path: "src/routes/+page.svelte"
      provides: "Stub overview page"
      contains: "NanoClaw Dashboard"
  key_links:
    - from: "svelte.config.js"
      to: "@sveltejs/adapter-node"
      via: "adapter import"
      pattern: "adapter-node"
    - from: "vite.config.ts"
      to: "tailwindcss"
      via: "@tailwindcss/vite plugin"
      pattern: "tailwindcss"
---

<objective>
Scaffold the SvelteKit 2.x project with @sveltejs/adapter-node, configure Tailwind 4 dark theme base, and create the dark-themed layout shell with sidebar nav and a stub overview page.

Purpose: This is the project foundation — all other plans depend on the SvelteKit scaffold existing.
Output: package.json, svelte.config.js, vite.config.ts, tailwind.config.js, tsconfig.json, src/app.css, src/routes/+layout.svelte, src/routes/+page.svelte, static/.gitkeep
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold SvelteKit project configuration files</name>
  <files>package.json, svelte.config.js, vite.config.ts, tailwind.config.js, tsconfig.json, static/.gitkeep</files>
  <read_first>.planning/research/STACK.md, .planning/research/SUMMARY.md</read_first>
  <action>
Create all project configuration files. Do NOT run `pnpm create svelte` — write the files directly.

**`package.json`** — exact content:
```json
{
  "name": "nanoclaw-dashboard",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
    "lint": "prettier --check . && eslint .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@sveltejs/adapter-node": "^5.2.12",
    "@sveltejs/kit": "^2.16.0",
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "@tailwindcss/vite": "^4.1.7",
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.10.0",
    "eslint": "^9.18.0",
    "eslint-config-prettier": "^10.0.1",
    "prettier": "^3.4.2",
    "prettier-plugin-svelte": "^3.3.3",
    "svelte": "^5.0.0",
    "svelte-check": "^4.1.4",
    "tailwindcss": "^4.1.7",
    "typescript": "^5.7.3",
    "vite": "^6.0.6"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "date-fns": "^3.6.0",
    "lucide-svelte": "^0.468.0",
    "zod": "^3.24.1"
  },
  "type": "module",
  "engines": {
    "node": ">=20"
  }
}
```

**`svelte.config.js`** — exact content:
```javascript
import adapter from '@sveltejs/adapter-node';
import { vitePreprocess } from '@sveltejs/vite-plugin-svelte';

/** @type {import('@sveltejs/kit').Config} */
const config = {
  preprocess: vitePreprocess(),
  kit: {
    adapter: adapter()
  }
};

export default config;
```

**`vite.config.ts`** — exact content:
```typescript
import { sveltekit } from '@sveltejs/kit/vite';
import tailwindcss from '@tailwindcss/vite';
import { defineConfig } from 'vite';

export default defineConfig({
  plugins: [
    tailwindcss(),
    sveltekit()
  ]
});
```

**`tailwind.config.js`** — exact content (Tailwind 4 minimal config):
```javascript
/** @type {import('tailwindcss').Config} */
export default {
  darkMode: 'class',
  content: ['./src/**/*.{html,js,ts,svelte}'],
  theme: {
    extend: {}
  },
  plugins: []
};
```

**`tsconfig.json`** — exact content:
```json
{
  "extends": "./.svelte-kit/tsconfig.json",
  "compilerOptions": {
    "allowJs": true,
    "checkJs": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "skipLibCheck": true,
    "sourceMap": true,
    "strict": true,
    "moduleResolution": "bundler"
  }
}
```

**`static/.gitkeep`** — create empty file so the directory exists in git.
  </action>
  <verify>
- package.json contains "@sveltejs/adapter-node" in devDependencies
- svelte.config.js imports from '@sveltejs/adapter-node' (not 'adapter-auto')
- vite.config.ts imports tailwindcss from '@tailwindcss/vite'
- tailwind.config.js has darkMode: 'class'
- tsconfig.json has "strict": true
  </verify>
  <acceptance_criteria>
    - "package.json contains \"@sveltejs/adapter-node\": \"^5.2.12\""
    - "svelte.config.js contains \"adapter-node\" import (NOT adapter-auto)"
    - "vite.config.ts contains \"@tailwindcss/vite\" import"
    - "tailwind.config.js contains \"darkMode: 'class'\""
    - "tsconfig.json contains \"strict\": true"
    - "static/.gitkeep exists"
  </acceptance_criteria>
  <done>All 6 configuration files exist with correct content</done>
</task>

<task type="auto">
  <name>Task 2: Dark theme CSS, layout shell, and overview stub</name>
  <files>src/app.css, src/routes/+layout.svelte, src/routes/+page.svelte</files>
  <read_first>.planning/research/STACK.md</read_first>
  <action>
Create the global CSS, layout component, and stub overview page.

**`src/app.css`** — exact content (Tailwind 4 CSS-variable dark theme):
```css
@import "tailwindcss";

@layer base {
  :root {
    --background: 224 71% 4%;
    --foreground: 213 31% 91%;
    --muted: 223 47% 11%;
    --muted-foreground: 215.4 16.3% 56.9%;
    --accent: 216 34% 17%;
    --accent-foreground: 210 40% 98%;
    --popover: 224 71% 4%;
    --popover-foreground: 215 20.2% 65.1%;
    --border: 216 34% 17%;
    --input: 216 34% 17%;
    --card: 224 71% 4%;
    --card-foreground: 213 31% 91%;
    --primary: 210 40% 98%;
    --primary-foreground: 222.2 47.4% 1.2%;
    --secondary: 222.2 47.4% 11.2%;
    --secondary-foreground: 210 40% 98%;
    --destructive: 0 63% 31%;
    --destructive-foreground: 210 40% 98%;
    --ring: 216 34% 17%;
    --radius: 0.5rem;
  }
}

@layer base {
  * {
    @apply border-[hsl(var(--border))];
  }
  body {
    @apply bg-[hsl(var(--background))] text-[hsl(var(--foreground))];
    font-feature-settings: "rlig" 1, "calt" 1;
  }
}
```

**`src/routes/+layout.svelte`** — exact content:
```svelte
<script lang="ts">
  import '../app.css';
  import { page } from '$app/stores';

  const navItems = [
    { href: '/', label: 'Overview' },
    { href: '/groups', label: 'Groups' },
    { href: '/sessions', label: 'Sessions' },
    { href: '/approvals', label: 'Approvals' }
  ] as const;

  let { children } = $props();
</script>

<svelte:head>
  <meta name="color-scheme" content="dark" />
</svelte:head>

<div class="flex h-screen overflow-hidden bg-[hsl(var(--background))] text-[hsl(var(--foreground))]">
  <aside class="flex flex-col w-[220px] shrink-0 border-r border-[hsl(var(--border))] bg-[hsl(var(--muted))]">
    <div class="flex items-center gap-2 px-4 h-14 border-b border-[hsl(var(--border))]">
      <span class="text-sm font-semibold tracking-tight">NanoClaw</span>
      <span class="text-xs px-1.5 py-0.5 rounded bg-[hsl(var(--accent))] text-[hsl(var(--muted-foreground))]">Dashboard</span>
    </div>
    <nav class="flex-1 py-4 px-2 space-y-1">
      {#each navItems as item}
        {@const isActive = $page.url.pathname === item.href || ($page.url.pathname.startsWith(item.href) && item.href !== '/')}
        <a
          href={item.href}
          class="flex items-center gap-3 px-3 py-2 rounded-md text-sm transition-colors
            {isActive
              ? 'bg-[hsl(var(--accent))] text-[hsl(var(--foreground))] font-medium'
              : 'text-[hsl(var(--muted-foreground))] hover:text-[hsl(var(--foreground))] hover:bg-[hsl(var(--accent))]'}"
        >
          {item.label}
        </a>
      {/each}
    </nav>
    <div class="px-4 py-3 border-t border-[hsl(var(--border))]">
      <p class="text-xs text-[hsl(var(--muted-foreground))]">v0.1.0</p>
    </div>
  </aside>
  <main class="flex-1 overflow-y-auto">
    {@render children()}
  </main>
</div>
```

**`src/routes/+page.svelte`** — stub content (will be replaced in Plan 01-04):
```svelte
<script lang="ts">
  // Stub overview page — will be enhanced in Plan 01-04 with health polling
</script>

<div class="p-8">
  <div class="max-w-4xl mx-auto">
    <div class="mb-8">
      <h1 class="text-2xl font-bold text-[hsl(var(--foreground))]">NanoClaw Dashboard</h1>
      <p class="mt-1 text-sm text-[hsl(var(--muted-foreground))]">System overview</p>
    </div>
    <div class="rounded-lg border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-6">
      <p class="text-sm text-[hsl(var(--muted-foreground))]">
        Health status initializing…
      </p>
    </div>
  </div>
</div>
```
  </action>
  <verify>
- src/app.css contains '@import "tailwindcss"' (Tailwind 4 — NOT @tailwind base)
- src/routes/+layout.svelte contains all four nav hrefs: '/', '/groups', '/sessions', '/approvals'
- src/routes/+page.svelte contains "NanoClaw Dashboard"
  </verify>
  <acceptance_criteria>
    - "src/app.css contains '@import \"tailwindcss\"' (Tailwind 4 syntax)"
    - "src/routes/+layout.svelte contains href=\"/groups\" and href=\"/sessions\" and href=\"/approvals\""
    - "src/routes/+page.svelte contains \"NanoClaw Dashboard\""
  </acceptance_criteria>
  <done>Dark theme shell with 4-item sidebar nav and stub overview page created</done>
</task>

</tasks>

<verification>
- [ ] package.json contains "@sveltejs/adapter-node" (NOT adapter-auto or adapter-static)
- [ ] svelte.config.js imports from '@sveltejs/adapter-node'
- [ ] vite.config.ts uses '@tailwindcss/vite' plugin
- [ ] src/app.css uses '@import "tailwindcss"' (Tailwind 4 syntax, NOT v3 @tailwind directives)
- [ ] +layout.svelte sidebar has exactly 4 nav items with hrefs: /, /groups, /sessions, /approvals
- [ ] +page.svelte is a stub (contains heading but no polling — polling added in 01-04)
- [ ] tailwind.config.js has darkMode: 'class'
</verification>

<success_criteria>
- All 9 files exist on disk with correct content
- svelte.config.js uses adapter-node (never adapter-auto or adapter-static)
- src/app.css uses Tailwind 4 @import syntax (not v3 @tailwind directives)
- Sidebar nav has exactly 4 links with correct hrefs
- +page.svelte is a stub (no polling logic — that's added in 01-04)
</success_criteria>

<output>After completion, create `.planning/phases/01-foundation-deploy/01-01-SUMMARY.md` with: files created, any deviations from the plan, and confirmation that all verification checks passed.</output>
