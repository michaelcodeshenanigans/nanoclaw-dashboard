---
phase: 08-responsive-nav
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/+layout.svelte
  - src/app.css
autonomous: true
requirements:
  - MOB-01
  - MOB-02
  - MOB-03
must_haves:
  truths:
    - "Svelte 5 runes ($state, $effect, $derived) are required — do NOT use legacy Svelte 4 stores or onDestroy"
    - "Tailwind 4.x uses CSS-native variables — use responsive prefixes (md:, sm:) directly, NOT a config file"
    - "The hamburger button must be visible ONLY on viewports ≤768px — use 'md:hidden' class"
    - "The sidebar must be hidden on mobile by default — use 'hidden md:flex' pattern on the aside"
    - "The mobile drawer uses fixed positioning + translate-x transition, NOT CSS display toggling"
    - "The backdrop must be a semi-transparent overlay behind the drawer, closing it on click"
    - "Every nav link in the drawer must close the drawer on click (set sidebarOpen = false)"
    - "The desktop layout (≥769px) must be VISUALLY UNCHANGED — sidebar stays fixed, hamburger hidden"
    - "pnpm is the ONLY package manager"
    - "No new npm packages — this is pure Tailwind + Svelte 5 runes work"
  artifacts:
    - path: "src/routes/+layout.svelte"
      provides: "Responsive layout with hamburger drawer on mobile, fixed sidebar on desktop"
      contains: "sidebarOpen"
    - path: "src/app.css"
      provides: "Global overflow-x guard to prevent horizontal page scroll"
      contains: "overflow-x"
  key_links:
    - from: "src/routes/+layout.svelte"
      to: "sidebarOpen state"
      via: "$state rune"
      pattern: "$state(false)"
---

<objective>
Transform the fixed 220px sidebar into a responsive layout: on desktop (≥769px) the sidebar stays exactly as-is; on mobile (≤768px) the sidebar is hidden and replaced by a hamburger button that opens a slide-in drawer. Closes on backdrop click or nav link tap. All pages must render without horizontal overflow at 375px.

Purpose: Phase 8 foundation — every page is immediately usable on mobile once this lands.
Output: Modified +layout.svelte (hamburger + drawer), small overflow-x guard in app.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
Read the current src/routes/+layout.svelte so you understand the exact structure you're modifying.
Read src/app.css to see existing global styles.
</task>

<task type="auto">
Rewrite src/routes/+layout.svelte to implement the responsive nav system.

The new layout must satisfy all four Phase 8 success criteria:

1. On ≤768px: fixed sidebar hidden, hamburger button visible in top bar.
2. Tapping hamburger opens a full-height drawer with nav links; tapping backdrop OR any nav link closes it.
3. Every existing page renders at 375px without horizontal page-level overflow.
4. Desktop layout (≥769px) is visually unchanged.

**Full implementation spec:**

```svelte
<script lang="ts">
  import '../app.css';
  import { page } from '$app/state';

  const navItems = [
    { href: '/', label: 'Overview' },
    { href: '/groups', label: 'Groups' },
    { href: '/sessions', label: 'Sessions' },
    { href: '/approvals', label: 'Approvals' },
    { href: '/dropped', label: 'Dropped' },
    { href: '/tasks', label: 'Tasks' }
  ] as const;

  let { children } = $props();
  let sidebarOpen = $state(false);

  function isActive(href: string): boolean {
    if (href === '/') return page.url.pathname === '/';
    return page.url.pathname.startsWith(href);
  }

  function closeDrawer() {
    sidebarOpen = false;
  }
</script>
```

Structure:
- Outer wrapper: `<div class="flex h-screen overflow-hidden bg-[hsl(var(--background))] text-[hsl(var(--foreground))]">`

**Mobile top bar** (visible only on mobile):
```html
<!-- Mobile header — only visible on small screens -->
<div class="md:hidden fixed top-0 left-0 right-0 z-30 h-14 flex items-center justify-between px-4 border-b border-[hsl(var(--border))] bg-[hsl(var(--muted))]">
  <div class="flex items-center gap-2">
    <span class="text-sm font-semibold tracking-tight">NanoClaw</span>
    <span class="text-xs px-1.5 py-0.5 rounded bg-[hsl(var(--accent))] text-[hsl(var(--muted-foreground))]">Dashboard</span>
  </div>
  <button
    onclick={() => sidebarOpen = true}
    class="flex items-center justify-center w-10 h-10 rounded-md hover:bg-[hsl(var(--accent))] text-[hsl(var(--foreground))]"
    aria-label="Open navigation"
  >
    <!-- Hamburger icon: three horizontal lines -->
    <svg width="20" height="20" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
      <line x1="2" y1="5" x2="18" y2="5"/>
      <line x1="2" y1="10" x2="18" y2="10"/>
      <line x1="2" y1="15" x2="18" y2="15"/>
    </svg>
  </button>
</div>
```

**Mobile drawer backdrop** (visible only when open on mobile):
```html
<!-- Backdrop -->
{#if sidebarOpen}
  <div
    class="md:hidden fixed inset-0 z-40 bg-black/50"
    onclick={closeDrawer}
    aria-hidden="true"
  ></div>
{/if}
```

**Mobile drawer** (slides in from left):
```html
<!-- Mobile drawer -->
<div class="md:hidden fixed top-0 left-0 bottom-0 z-50 w-[220px] flex flex-col border-r border-[hsl(var(--border))] bg-[hsl(var(--muted))] transform transition-transform duration-200 {sidebarOpen ? 'translate-x-0' : '-translate-x-full'}">
  <div class="flex items-center gap-2 px-4 h-14 border-b border-[hsl(var(--border))] shrink-0">
    <span class="text-sm font-semibold tracking-tight">NanoClaw</span>
    <span class="text-xs px-1.5 py-0.5 rounded bg-[hsl(var(--accent))] text-[hsl(var(--muted-foreground))]">Dashboard</span>
  </div>
  <nav class="flex-1 py-4 px-2 space-y-1">
    {#each navItems as item}
      <a
        href={item.href}
        onclick={closeDrawer}
        class="flex items-center gap-3 px-3 py-2 rounded-md text-sm transition-colors
          {isActive(item.href)
            ? 'bg-[hsl(var(--accent))] text-[hsl(var(--foreground))] font-medium'
            : 'text-[hsl(var(--muted-foreground))] hover:text-[hsl(var(--foreground))] hover:bg-[hsl(var(--accent))]'}"
      >
        {item.label}
      </a>
    {/each}
  </nav>
  <div class="px-4 py-3 border-t border-[hsl(var(--border))] shrink-0">
    <p class="text-xs text-[hsl(var(--muted-foreground))]">v0.1.0</p>
  </div>
</div>
```

**Desktop sidebar** (hidden on mobile, unchanged visually):
```html
<!-- Desktop sidebar — hidden on mobile -->
<aside class="hidden md:flex flex-col w-[220px] shrink-0 border-r border-[hsl(var(--border))] bg-[hsl(var(--muted))]">
  <div class="flex items-center gap-2 px-4 h-14 border-b border-[hsl(var(--border))]">
    <span class="text-sm font-semibold tracking-tight">NanoClaw</span>
    <span class="text-xs px-1.5 py-0.5 rounded bg-[hsl(var(--accent))] text-[hsl(var(--muted-foreground))]">Dashboard</span>
  </div>
  <nav class="flex-1 py-4 px-2 space-y-1">
    {#each navItems as item}
      <a
        href={item.href}
        class="flex items-center gap-3 px-3 py-2 rounded-md text-sm transition-colors
          {isActive(item.href)
            ? 'bg-[hsl(var(--accent))] text-[hsl(var(--foreground))] font-medium'
            : 'text-[hsl(var(--muted-foreground))] hover:text-[hsl(var(--foreground))] hover:bg-[hsl(var(--accent))]'}"
      >
        {item.label}
      </a>
    {/each}
  </nav>
  <div class="px-4 py-3 border-t border-[hsl(var(--border))]">
    <p class="text-xs text-[hsl(var(--muted-foreground))]">v0.1.0</p>
  </div>
</aside>
```

**Main content area** — on mobile, add top padding to account for the fixed mobile header:
```html
<main class="flex-1 overflow-y-auto overflow-x-hidden md:pt-0 pt-14">
  {@render children()}
</main>
```

The complete file is an assembly of all the above blocks in order: script, svelte:head, outer div, mobile header, backdrop, mobile drawer, desktop sidebar, main.
</task>

<task type="auto">
Add a global overflow-x guard to src/app.css so individual pages can't accidentally cause horizontal scroll on mobile.

Add this rule to the file (after any existing :root or body rules, before component-specific styles):

```css
html, body {
  overflow-x: hidden;
}
```

Do NOT remove or change any existing rules — only append this.
</task>

<task type="auto">
Run `pnpm build` in /workspace/agent/nanoclaw-dashboard to verify the TypeScript compiles clean with no errors.

If the build fails:
- Check for TypeScript errors in the output
- Fix any issues in the modified files
- Re-run build until it succeeds

Do NOT proceed to the summary until the build is clean.
</task>

<task type="auto">
Write the summary file at .planning/phases/08-responsive-nav/08-01-SUMMARY.md using the summary template. Include:
- What changed (layout file + app.css)
- The responsive breakpoint strategy used (md: = 768px)
- Build result (clean/errors fixed)
</task>

</tasks>
